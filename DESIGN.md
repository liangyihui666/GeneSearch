# 基因速查设计说明

## 固定的视觉世界

- 继承 OncoRounds 系列：蓝紫青光晕、浅色空气感背景、品牌球体和克制的半透明表面。
- 竞品截图仅用于确认“基因—药物—癌种”检索关系，不复刻其视觉样式。
- 主要信息层级固定为：搜索方向 → 统一搜索 → 国内证据结果 → 标志物信息总览 → 变异/表达分组 → 临床结论与适用条件 → 药物关系 → NMPA 批准来源 → 医保/商保目录来源。

## 响应式策略

- 产品为移动优先 H5，基准宽度 390px；桌面端保持最大 480px 的专注工作壳体。
- 首屏必须同时出现品牌、搜索方向、搜索框和至少一条可识别的证据结果。
- 横向标签可滚动，正文不产生横向溢出。

## 交互与可访问性

- 所有核心触控区域不小于 44×44px。
- 搜索方向使用可切换按钮组和 `aria-pressed` 状态，避免不完整的 tab 语义。
- 进入详情后焦点移动到基因标题；返回列表后恢复到原触发按钮。
- 结果卡支持整卡点击进入详情；卡内药物区必须隔离整卡跳转，药物按钮只执行反向检索，并保留显式详情按钮作为键盘入口。
- 卡片默认展示前 4 个唯一药物；“展开全部 N 项/收起”仅改变卡内药物列表，不触发详情或反向检索。
- 药物详情使用原生折叠控件，长药名、多个目录标签及来源说明在 320px 下不得横向溢出。
- 键盘焦点轮廓、选区、插入光标、滚动条与品牌色保持一致。

## 医学内容门禁

- 当前仅展示国内官方来源样例，不出现 FDA、NCCN、ESMO 等国外数据结论。
- “官方来源已核对”只代表对应批准查询与目录条目已核对；医学文案仍需医学审核。
- 当前演示内容必须标注为 `Demo v0.2`，并明确显示“内容更新 2026-09-01”；两项均属于发布门禁，不得省略或改写为来源发布日期。
- 目录有效期、目录页码和支付限制只能出现在医保/商保区；批准状态与适用人群只能由独立 NMPA 来源支持。
- 列表和详情均展示内容版本与内容更新时间；来源发布日期单独展示，不与内容更新时间混用。
- Demo 不替代说明书、监管信息或临床判断。
